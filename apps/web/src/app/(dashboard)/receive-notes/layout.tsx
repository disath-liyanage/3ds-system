import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "GRN"
};

export default function ReceiveNotesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
